{
  "dependencies": {
    "@calcit/procs": "^0.3.0-a10"
  },
  "scripts": {
    "test": "cr --emit-js --init-fn:respo-router.test/run-tests --reload-fn:respo-router.test/reload! --once && target=node entry=./test.js yarn webpack && node js-out/bundle.js"
  },
  "devDependencies": {
    "vite": "^2.2.3",
    "webpack": "^5.36.0",
    "webpack-cli": "^4.6.0"
  },
  "version": "0.5.3"
}

# Release 0.8.10

- Upgrade Calcit and `@calcit/procs` to 0.13.77, align the router with the released Respo 0.16.93, and refresh the Yarn lockfile.
- Normalize a missing route segment to an empty string before pattern comparison so the route matcher follows Calcit’s current `Option<String>` contract.
- 将 Calcit 与 `@calcit/procs` 升级到 0.13.77，并对齐已发布的 Respo 0.16.93、刷新 Yarn 锁文件。
- 在 pattern 比较前将缺失路由段规范为空字符串，使路由匹配器符合当前 Calcit 的 `Option<String>` 契约。

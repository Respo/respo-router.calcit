# Release 0.8.8

- Upgrade Calcit and `@calcit/procs` to 0.13.75 and align Respo with 0.16.89 so strict downstream dependency graphs can consume the render-boundary fixes.
- Explicitly unwrap required route tuple positions before tag comparison, path joining, and pattern filling.
- 将 Calcit 与 `@calcit/procs` 升级到 0.13.75，并将 Respo 对齐到 0.16.89；在 tag 比较、路径拼接和 pattern 填充前显式解包必需的 route tuple 位置。
